name: CI
permissions:
  contents: read

on:
  push:
    branches:
      - "*"
  pull_request:
    branches:
      - main

jobs:
  quality-checks:
    # TODO: Add a deploy job for `main` that publishes the image to GHCR after this job passes.
    # TODO: Log in to Azure and update the ACA image there once deployment wiring is ready.
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6.0.2

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2.1.3
        with:
          bun-version: 1.3.10

      - name: Install dependencies
        run: bun ci

      - name: Biome CI
        run: bunx biome ci

      - name: Run tests with coverage
        run: bun run test:coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5.5.2
        # TODO: Re-check the README coverage badge after the default branch starts uploading this report.
